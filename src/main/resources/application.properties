# Perfil activo
spring.profiles.active=prod

# Configuración de la base de datos
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false

# Configuración de Hikari Pool
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Configuración de Seguridad
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin}
spring.main.allow-bean-definition-overriding=true
app.jwtSecret=${JWT_SECRET:MisakGumbShop_2024_Artesanias_Textiles_Seguridad_JWT_Clave_Secreta_9876543210_abcdefghijklmnopqrstuvwxyz_ABCDEFGHIJKLMNOPQRSTUVWXYZ}
app.jwtExpirationInMs=86400000

# Configuración de subida de archivos
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Configuración de correo
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:plantillajs@gmail.com}
spring.mail.password=${MAIL_PASSWORD:rzksmfshwevkmjfv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.debug=false

# Configuración de logging
logging.level.root=ERROR
logging.level.org.springframework.web=ERROR
logging.level.org.hibernate=ERROR
logging.level.org.springframework.security=ERROR
logging.level.com.misakguambshop.app=INFO

# Configuración de la aplicación
spring.application.name=misakguambshop-backend

# Configuración de Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dicuoxtbg}
cloudinary.api-key=${CLOUDINARY_API_KEY:821733451974473}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:2VCmAHo1_LX7kHMZCE7jCmsqW6I}

# Configuración de PayU
payu.api.url=${PAYU_API_URL:https://sandbox.api.payulatam.com/payments-api/4.0/service.cgi}
payu.api.key=${PAYU_API_KEY:aw0pOm0ysRSfkELKvf9mHkl4ZV}
payu.api.login=${PAYU_API_LOGIN:tNRWA638S2U0wVV}
payu.merchant.id=${PAYU_MERCHANT_ID:1015882}
payu.account.id=${PAYU_ACCOUNT_ID:1024812}
payu.country=${PAYU_COUNTRY:COP}
payu.test=${PAYU_TEST:false}

# Configuración del servidor
server.port=${PORT:8080}
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain
server.compression.min-response-size=1024

# URL del frontend
app.base-url=${FRONTEND_URL:https://misak-guamb-shop-front-git-develop-my-team-f83432a3.vercel.app}

# Configuración adicional de seguridad
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true